const config = {
    prefix: '!', // Command prefix for message-based commands
    ownerId: 'YOUR_DISCORD_USER_ID', // Your Discord user ID for owner-only commands
    defaultVolume: 50, // Default volume level for music playback
    maxQueueSize: 100, // Maximum number of songs in the queue
    supportServer: 'https://discord.gg/YOUR_SUPPORT_SERVER_LINK', // Link to your support server
};

export default config;